import 'package:flutter/material.dart';

class HealthBreedingManagement extends StatelessWidget {
  const HealthBreedingManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Health & Breeding')),
      body: const Center(child: Text('❤️🐮 Welcome to Health & Breeding Management')),
    );
  }
}
